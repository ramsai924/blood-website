#topalerts{
position: fixed;
top: 0%;
width: 100%;
height: 100px;
transition: 0.5s;
background-color: green;
}
#popupss{
position: absolute;
top: 10%;
left: 40%;
transform: translate(-50% -50%);
}
@media (max-width: 575.98px) {
#alerts{
position: fixed;
margin-top: 15%;
}
#popupss{
left: 10%;
}
}

window.addEventListener('scroll', function () {
if (window.pageYOffset > 30) {
document.querySelector("#topalerts").style.top = "0px"
console.log("kj")
} else if (window.pageYOffset < 500) { document.getElementById("topalerts").style.top="-15%" } }); function
    closealert(){ document.getElementById("topalerts").style.top="-15%" } setTimeout(()=> {
    document.getElementById("alerts").style.display = "none"
    }, 3000);


<div id="topalerts">
    <div class="container">
        <div id="popupss">
            <div>
                <p>Post patient details we willget back to you</p>
            </div>
            <div style="margin-top: 20px;">
                <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#myModal"
                    style="background: transparent;">
                    <span style="color: black;font-weight: bold;">Sign in</span>
                </button>

                <button class="btn btn-primary" onclick="closealert()">close</button>
            </div>
        </div>
    </div>

</div>